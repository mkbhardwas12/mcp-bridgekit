## What & why

<!-- One or two sentences. Link the issue if there is one: Fixes #123 -->

## Changes

-

## Checklist

- [ ] `uv run pytest -q` passes; new behaviour has a test
- [ ] `uv run ruff check src tests examples` is clean
- [ ] Docs / README updated if config, endpoints or error codes changed
- [ ] `CHANGELOG.md` entry added under **Unreleased** with my handle
- [ ] Nothing in this PR lets an HTTP caller control what BridgeKit spawns without an allowlist
